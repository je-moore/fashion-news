import React from 'react'
import { Typography } from '@material-ui/core'

function NewsHeader({ article }) {
  return (
    <Typography color="textPrimary" variant="h1" align="center">
      Fashion News
    </Typography>
  )
}

export default NewsHeader
